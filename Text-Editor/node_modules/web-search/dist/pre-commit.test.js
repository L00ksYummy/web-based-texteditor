"use strict";
var __importDefault = (this && this.__importDefault) || function (mod) {
    return (mod && mod.__esModule) ? mod : { "default": mod };
};
exports.__esModule = true;
var data_json_1 = __importDefault(require("./data.json"));
test("data.json should be follow WebSearchData", function () {
    expect(data_json_1["default"]).toBeDefined();
    expect(data_json_1["default"]).toHaveProperty("websites");
    expect(data_json_1["default"].websites).toBeDefined();
    expect(data_json_1["default"].websites).toBeInstanceOf(Array);
    data_json_1["default"].websites.forEach(function (website) {
        expect(website).toHaveProperty("name");
        expect(website.name).toBeDefined();
        expect(website).toHaveProperty("url_prefix");
        expect(website.url_prefix).toBeDefined();
    });
});
